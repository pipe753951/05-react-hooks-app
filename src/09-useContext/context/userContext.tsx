import { type PropsWithChildren } from "react";

export const UserContextProvider = function ({ children }: PropsWithChildren) {
  // const [name, setName] = useState("Gabriel García Márquez");

  return children;
};
